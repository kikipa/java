package algorithms.structure.graph.undirected;

/**
 * 路径的API
 * */
public class Paths {
    /**
     * 在G中找出所有起点为s的路径
     * */
    Paths(Graph G, int s){

    }

    /**
     * 是否存在从s到v的路径
     * */
    boolean hasPathTo(int v){
        return false;
    }

    /**
     * s到v的路径，如果不存在则返回null
     * */
    Iterable<Integer> pathTo(int v){
        return null;
    }
}
